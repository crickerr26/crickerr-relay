# nixpacks.toml
# Tells Railway/Nixpacks to install FFmpeg alongside Node.js

[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node server.js"
